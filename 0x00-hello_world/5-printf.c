#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
char alxpro[] = "with proper grammar, but the outcome is a piece of art,";

printf("%s\n", alxpro);

return (0);
}
